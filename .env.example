# Application
APP_NAME=go-employee-analytics
PORT=8080
LOG_LEVEL=6

# Database Configuration
DB_USERNAME=
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
DB_NAME=employee_analytics
DB_POOL_IDLE=10
DB_POOL_MAX=100
DB_POOL_LIFETIME=300

# Migrations
OUTPUT_DIR=output

# Tables to drop before running migrations (comma-separated)
DROP_TABLE_NAMES=annual_reviews,employees
